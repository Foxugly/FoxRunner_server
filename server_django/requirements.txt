# Dependencies required by the Django rewrite. Merge into the top-level
# requirements.txt at swap time (phase 13). Versions are intentionally loose
# here; pin via pip-compile when the migration is complete.
django>=5.0
django-ninja>=1.3
djoser>=2.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-redis>=5.4
django-cors-headers>=4.5
django-ratelimit>=4.1
psycopg[binary]>=3.2
