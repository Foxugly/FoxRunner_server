# Runtime dependency inputs. Keep this list minimal; development-only tools
# (ruff, coverage, pre-commit) belong in requirements-dev.txt so they don't
# end up in the production Docker image.
#
# This file is the input list — the pinned versions live in
# requirements.lock and that is what CI and the Dockerfile install.
# Regenerate with `make relock` after editing this file.
requests
selenium
truststore
colorama
python-dotenv
jsonschema
fastapi
uvicorn
httpx
fastapi-users[sqlalchemy]
aiosqlite
alembic
asyncpg
celery[redis]
flower
