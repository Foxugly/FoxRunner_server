# Runtime dependencies. Keep this list minimal; development-only tools (ruff,
# coverage, pre-commit) belong in requirements-dev.txt so they don't end up in
# the production Docker image.
#
# Versions are intentionally unpinned here to keep the initial setup simple.
# Before a production rollout, regenerate a pinned lockfile with:
#   pip-compile --generate-hashes --output-file=requirements.lock requirements.txt
# and install from the lockfile in CI / Dockerfile.
requests
selenium
truststore
colorama
python-dotenv
jsonschema
fastapi
uvicorn
httpx
fastapi-users[sqlalchemy]
aiosqlite
alembic
asyncpg
celery[redis]
flower
