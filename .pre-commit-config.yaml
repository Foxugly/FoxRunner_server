repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.11
    hooks:
      # ruff-check and ruff-format are the canonical hook ids since 0.15; the
      # legacy `ruff` alias still works but prints a deprecation banner.
      - id: ruff-check
      - id: ruff-format
